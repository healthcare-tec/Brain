# ─────────────────────────────────────────────────────────────────────────────
# Charlie — Pacotes que FALTAM no jupenv
# (após migração para SQLite — sem PostgreSQL)
# ─────────────────────────────────────────────────────────────────────────────
#
# Pacotes JÁ instalados no jupenv (não precisam ser instalados):
#   fastapi           0.116.1  ✓
#   uvicorn           0.35.0   ✓
#   sqlalchemy        2.0.42   ✓
#   pydantic          2.11.7   ✓
#   pydantic-settings 2.13.1   ✓
#   httpx             0.28.1   ✓
#   python-multipart  0.0.20   ✓
#
# Pacotes AUSENTES — instalar com:
#   pip install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements-missing.txt
#
aiosqlite>=0.20.0
aiofiles>=24.0.0
alembic>=1.13.0
pytest>=8.0.0
pytest-asyncio>=0.24.0

# AI Cognitive Layer — necessário para integração com OpenAI
openai>=1.40.0
