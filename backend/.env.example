# ─────────────────────────────────────────────────────────────────────────────
# Charlie — Cognitive Operating System
# Backend environment configuration
#
# Copy this file to .env and fill in your values:
#   cp backend/.env.example backend/.env
#
# The .env file is listed in .gitignore and will NEVER be committed.
# ─────────────────────────────────────────────────────────────────────────────

# ── Database (SQLite — no server required) ───────────────────────────────────
# Default path is backend/charlie.db (auto-created on first run)
# You can change the path if needed, but SQLite is recommended.
DATABASE_URL=sqlite+aiosqlite:////absolute/path/to/Brain/backend/charlie.db
DATABASE_URL_SYNC=sqlite:////absolute/path/to/Brain/backend/charlie.db

# ── Knowledge base ───────────────────────────────────────────────────────────
KNOWLEDGE_BASE_PATH=/absolute/path/to/Brain/knowledge

# ── Debug mode ───────────────────────────────────────────────────────────────
DEBUG=false

# ── OpenAI — AI Cognitive Layer ──────────────────────────────────────────────
# Add your OpenAI API key here to enable the AI features.
# Without this key, Charlie uses heuristic fallbacks (still fully functional).
#
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ── AI Models (optional — defaults shown) ────────────────────────────────────
# L1 Classification — fast and cheap
AI_MODEL_L1=gpt-4o-mini

# L2 Interpretation — balanced
AI_MODEL_L2=gpt-4o-mini

# L3 Analysis — most capable (used for pattern detection and weekly review)
AI_MODEL_L3=gpt-4o

# ── AI Parameters (optional — defaults shown) ────────────────────────────────
AI_MAX_TOKENS_L1=256
AI_MAX_TOKENS_L2=1024
AI_MAX_TOKENS_L3=2048

AI_TEMPERATURE_L1=0.1
AI_TEMPERATURE_L2=0.4
AI_TEMPERATURE_L3=0.7
